#if defined _config_char_spawn_included
    #endinput
#endif

#define _config_char_spawn_included

enum
{
    CHARACTER_INVALID_SPAWN_TYPE,
    CHARACTER_SPAWN,
    CHARACTER_SWITCH,
    CHARACTER_SPECTATE,
    CHARACTER_DEAD
}